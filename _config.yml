title: FromRMIT
email: hello@fromrmit.com
description: >- # this means to ignore newlines until "baseurl:"
    RMIT Vietnam Design and Media Student Directory
baseurl: '' # the subpath of your site, e.g. /blog
url: '' # the base hostname & protocol for your site, e.g. http://example.com

collections:
    profiles:
        output: true
        title: Profiles
        permalink: /profile/:slug
    featured:
        output: true
        title: Featured
        permalink: /featured/:slug
    posts:
        output: true
        permalink: /blog/:slug
